export enum DATABASE_TYPE {
  BIGQUERY = 'bigquery',
  SNOWFLAKE = 'snowflake'
}

export enum TREE_LEVEL {
  DATABASE = 'database',
  SCHEMA = 'schema',
  TABLE = 'table',
  COLUMNS = 'columns',
  CHECKS = 'checks',
  COLUMN = 'column',
  ROOT = 'root',
  CHECK = 'check'
}
