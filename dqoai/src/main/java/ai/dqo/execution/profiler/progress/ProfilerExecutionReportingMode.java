package ai.dqo.execution.profiler.progress;

/**
 * Enumeration of profiler execution reporting modes. Each one maps to a profiler execution progress listener.
 */
public enum ProfilerExecutionReportingMode {
    silent
}
