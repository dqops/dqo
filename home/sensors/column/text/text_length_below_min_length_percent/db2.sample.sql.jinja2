{% import '/dialects/db2.sql.jinja2' as lib with context -%}
{%- call lib.render_error_sampler() -%}
    LENGTH(CAST({{ lib.render_target_column('analyzed_table')}} AS VARCHAR(4096))) < {{(parameters.min_length)}}
{%- endcall -%}