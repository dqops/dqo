{% import '/dialects/presto.sql.jinja2' as lib with context -%}
{%- call lib.render_error_sampler() -%}
    LENGTH(CAST({{ lib.render_target_column('analyzed_table') }} AS VARCHAR)) < {{(parameters.min_length)}}
{%- endcall -%}