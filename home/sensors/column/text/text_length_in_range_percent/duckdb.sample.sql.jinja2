{% import '/dialects/duckdb.sql.jinja2' as lib with context -%}
{%- call lib.render_error_sampler(wrap_condition = 'NOT') -%}
    LENGTH( CAST({{ lib.render_target_column('analyzed_table') }} AS VARCHAR) ) BETWEEN {{parameters.min_length}} AND {{parameters.max_length}}
{%- endcall -%}