{% import '/dialects/sqlserver.sql.jinja2' as lib with context -%}
{%- call lib.render_error_sampler(wrap_condition = 'NOT') -%}
    TRY_CAST({{ lib.render_target_column('analyzed_table')}} AS DATE) IS NOT NULL
{%- endcall -%}