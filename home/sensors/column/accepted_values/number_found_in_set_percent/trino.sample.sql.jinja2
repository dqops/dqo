{% import '/dialects/trino.sql.jinja2' as lib with context -%}
{%- call lib.render_error_sampler(wrap_condition = 'NOT') -%}
    {{ lib.render_target_column('analyzed_table') }} IN ({{parameters.expected_values|join(', ')}} }})
{%- endcall -%}