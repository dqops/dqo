# yaml-language-server: $schema=https://cloud.dqops.com/dqo-yaml-schema/SensorDefinitionYaml-schema.json
apiVersion: dqo/v1
kind: sensor
spec:
  fields:
  - field_name: min_value
    display_name: min_value
    help_text: "This field can be used to define custom value. In order to define\
      \ custom value, user should write correct value as an integer. If value is not\
      \ defined by user then default value is 0"
    data_type: integer
    required: true
