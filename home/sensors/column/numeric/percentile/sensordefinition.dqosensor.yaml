# yaml-language-server: $schema=https://cloud.dqops.com/dqo-yaml-schema/SensorDefinitionYaml-schema.json
apiVersion: dqo/v1
kind: sensor
spec:
  fields:
  - field_name: percentile_value
    display_name: percentile_value
    help_text: "Median (50th percentile), must equal 0.5"
    data_type: double
    required: true
