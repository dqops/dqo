# yaml-language-server: $schema=https://cloud.dqops.com/dqo-yaml-schema/CheckDefinitionYaml-schema.json
apiVersion: dqo/v1
kind: check
spec:
  sensor_name: column/numeric/percentile
  rule_name: change/change_percent_30_days
  help_text: Verifies that the median in a column changed in a fixed rate since the
    last readout from the last month.
  friendly_name: Maximum relative change in the median of numeric values vs 30 days
    ago
  default_severity: warning
