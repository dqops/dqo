# yaml-language-server: $schema=https://cloud.dqops.com/dqo-yaml-schema/CheckDefinitionYaml-schema.json
apiVersion: dqo/v1
kind: check
spec:
  sensor_name: column/strings/string_min_length
  rule_name: comparison/min_value
  help_text: Verifies that the length of string in a column does not fall below the
    minimum accepted length.
  standard: true
